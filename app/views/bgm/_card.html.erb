<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title">
      <%= item.name %>
      <% est = (DateTime.now.to_date - item.last_updated.to_date).to_i %>
      <a class="btn btn-info btn-xs pull-right" data-toggle="tooltip" title="" data-placement="top" data-original-title="<%= item.last_updated %>">
        <%= fa_icon "clock-o" %> <%= est %> <%= if est > 1 then "days" else "day" end %>
      </a>
    </h3>
  </div>
  <div class="panel-body">
    <div class="thumbnail">
      <%= image_tag bangumi_cover_url(item) %>
    </div>
    <div class="btn-group">
      <% i = item.items.sort_by {| obj | obj.ep_id }.reverse %>
      <%= link_to "#{fa_icon "play"} EP ##{i.first.ep_id}".html_safe, bangumi_play_path(i.first), class: 'btn btn-sm btn-success'%>
      <% i.delete(i.first) %>
      <div class="btn-group">
        <a class="btn btn-sm btn-primary dropdown-toggle" data-toggle="dropdown">
          <%= fa_icon "history" %>&nbsp;
          <%= fa_icon "caret-down" %>
        </a>
        <ul class="dropdown-menu">
          <% i.each do | ep | %>
          <li><%= link_to "EP ##{ep.ep_id}".html_safe, bangumi_play_path(ep.id) %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
